---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "opnsense_bind_primary_domain_update_key Resource - terraform-provider-opnsense"
subcategory: ""
description: |-
  Attaches one TSIG key to an existing BIND primary zone for RFC2136 updates without owning the zone or its other update keys. Delete removes only this membership.
---

# opnsense_bind_primary_domain_update_key (Resource)

Attaches one TSIG key to an existing BIND primary zone for RFC2136 updates without owning the zone or its other update keys. Delete removes only this membership.

## Example Usage

```terraform
resource "opnsense_bind_primary_domain_update_key" "acme" {
  domain_id     = opnsense_bind_primary_domain.public.id
  update_key_id = opnsense_bind_tsig_key.acme.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain_id` (String) UUID of the existing primary zone.
- `update_key_id` (String) UUID of the TSIG key to add to the zone update-key membership.

### Read-Only

- `id` (String) Attachment identity in `domain_id/update_key_id` form.
