---
page_title: "opnsense_caddy_handler Resource - terraform-provider-opnsense"
subcategory: Caddy
description: |-
  Manages a Caddy HTTP handler and its upstream reverse-proxy configuration.
---

# opnsense_caddy_handler (Resource)

Manages a Caddy HTTP handler and its upstream reverse-proxy configuration.

## Example Usage

```terraform
resource "opnsense_caddy_handler" "application" {
  domain_id         = opnsense_caddy_domain.internal.id
  upstream_domains  = ["10.20.0.10", "10.20.0.11"]
  upstream_port     = 8443
  upstream_protocol = "https"

  tls_trust_ca_ref_id = var.internal_ca_ref_id
  tls_server_name     = "backend.internal.example"

  load_balancing_policy = "round_robin"
  health_uri            = "/health"
  health_status         = "200"
  description           = "Application backends"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain_id` (String) UUID of the Caddy domain containing this handler.
- `upstream_domains` (Set of String) One or more upstream IP addresses, hostnames, or FQDNs.

### Optional

- `access_list_id` (String) Optional Caddy access-list UUID. Defaults to `""`.
- `basic_auth_ids` (Set of String) Caddy basic-auth entry UUIDs. Defaults to an empty set.
- `description` (String) Optional description. Defaults to `""`.
- `directive` (String) Handler directive: `reverse_proxy` or `redir`. Defaults to `reverse_proxy`.
- `enabled` (Boolean) Whether to enable this handler. Defaults to `true`.
- `forward_auth` (Boolean) Whether this handler is used for forward authentication. Defaults to `false`.
- `handler_type` (String) Handler type: `handle` or `handle_path`. Defaults to `handle`.
- `header_ids` (Set of String) Caddy header-manipulation UUIDs. Defaults to an empty set.
- `health_body` (String) Expected response-body substring for active health checks. Defaults to `""`.
- `health_fails` (Number) Consecutive failed checks required. `-1` uses the Caddy default.
- `health_follow_redirects` (Boolean) Whether active health checks follow redirects. Defaults to `false`.
- `health_interval` (Number) Active health-check interval in seconds. `-1` uses the Caddy default.
- `health_passes` (Number) Consecutive successful checks required. `-1` uses the Caddy default.
- `health_port` (Number) Active health-check port. `-1` leaves it unset.
- `health_status` (String) Expected active health-check HTTP status, for example `200` or `2xx`. Defaults to `""`.
- `health_timeout` (Number) Active health-check timeout in seconds. `-1` uses the Caddy default.
- `health_upstream` (String) Dedicated active health-check upstream. Defaults to `""`.
- `health_uri` (String) Active health-check URI. Defaults to `""`.
- `http_keepalive` (Number) HTTP keepalive duration in seconds. `-1` uses the Caddy default.
- `http_version` (String) Optional HTTP version restriction: `http1`, `http2`, or `http3`. Empty uses Caddy defaults.
- `load_balancing_policy` (String) Load-balancing policy. Empty means random; alternatives include `first`, `round_robin`, `least_conn`, `ip_hash`, `client_ip_hash`, and `uri_hash`.
- `load_balancing_retries` (Number) Number of load-balancing retries. `-1` uses the Caddy default.
- `load_balancing_try_duration` (Number) Load-balancing try duration in seconds. `-1` uses the Caddy default.
- `load_balancing_try_interval` (Number) Load-balancing try interval in seconds. `-1` uses the Caddy default.
- `passive_health_fail_duration` (Number) Passive health failure duration in seconds. `-1` uses the Caddy default.
- `passive_health_max_fails` (Number) Passive health maximum failures. `-1` uses the Caddy default.
- `passive_health_unhealthy_latency` (Number) Unhealthy latency threshold in seconds. `-1` uses the Caddy default.
- `passive_health_unhealthy_request_count` (Number) Unhealthy request-count threshold. `-1` uses the Caddy default.
- `passive_health_unhealthy_status` (String) HTTP status or status class considered unhealthy, for example `404` or `5xx`. Defaults to `""`.
- `path` (String) Optional request path matcher starting with `/`. Defaults to `""`.
- `subdomain_id` (String) Optional UUID of a Caddy subdomain. Defaults to `""`.
- `tls_insecure_skip_verify` (Boolean) Whether to skip upstream TLS certificate verification. Defaults to `false`; prefer `tls_trust_ca_ref_id`.
- `tls_server_name` (String) TLS server name used to verify the upstream certificate. Defaults to `""`.
- `tls_trust_ca_ref_id` (String) Reference ID of an OPNsense CA trusted for the HTTPS upstream. Defaults to `""`.
- `upstream_path` (String) Optional path prepended to upstream requests. Defaults to `""`.
- `upstream_port` (Number) Optional upstream port. `-1` leaves it unset.
- `upstream_protocol` (String) Upstream protocol: `http`, `https`, or `h2c`. Defaults to `http`.

### Read-Only

- `id` (String) UUID of the handler.

## Import

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import) to import opnsense_caddy_handler using its ID.

```terraform
import {
  to = opnsense_caddy_handler.example
  id = "<opnsense-resource-id>"
}
```

Using `terraform import`:

```console
% terraform import opnsense_caddy_handler.example <opnsense-resource-id>
```
